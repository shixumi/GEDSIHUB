<!-- In your Admin Dashboard Razor Page -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/6.0.0/signalr.min.js"></script>
<script>
    const connection = new signalR.HubConnectionBuilder()
        .withUrl("/analyticsHub")
        .build();

    connection.on("UpdateActiveUsers", function (count) {
        document.getElementById("activeUsersCount").innerText = count;
    });

    connection.start().catch(function (err) {
        return console.error(err.toString());
    });
</script>

<div>
    <h3>Current Active Users: <span id="activeUsersCount">0</span></h3>
</div>
